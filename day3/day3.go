package day3

// Assumes we're always given valid input
func readInput(infile string) {
	// filehandle, err := os.Open(infile)
	// if err != nil {
	// 	log.Fatal(err)
	// }

	// scanner := bufio.NewScanner(filehandle)

	// for scanner.Scan() {
	// 	line := scanner.Text()
	// }
}

func Run(filename string) {
}
